# LunaBridge-Paper never owns Minecraft chat commands or a Discord client.
config-version: 1
server:
  id: "lobby"
network:
  velocity: true
  # Set the same random 16+ character value as the Velocity config. Do not commit it.
  shared-pass: ""
bridges:
  global:
    luna-channel: "global"
    discord-channel-id: ""
limits:
  network-outbox: 256
  dedup-entries: 10000
