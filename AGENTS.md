# LunaBridge agent instructions

- バージョン管理を必ず行うこと。
- コードまたは配布物を変更する前に、必ず `VERSIONING.md` を読み、その変更セットで必要なバージョン更新を行うこと。
- バージョンを更新せずにコード変更をコミットしてはならない。例外は、配布物へ影響しない文書・テストのみの変更に限る。
- `projectVersion`、Paper/Velocityメタデータ、成果物名の整合性をビルド後に確認すること。
